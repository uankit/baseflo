"""Auth subsystem.

Per docs/40-features/AUTH.md. M0 ships dev-mode header-based auth so the rest
of the system can plumb tenant context end-to-end. Real Lucia-style sessions,
OAuth and SSO are not available in v1.
"""
