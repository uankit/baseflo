# Chart Grammar Plane

The Chart Grammar Plane converts Baseflo chart specs into Vega-Lite JSON.

Agents may choose chart intent, but the frontend receives a typed grammar spec
with materialized values and lineage refs. The UI should render these specs
directly instead of parsing prose.
