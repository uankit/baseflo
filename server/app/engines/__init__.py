"""Deterministic engines: schema IR/DDL, analytics, exports, simulation, ...

Per docs/05-coding-rules.md §1.1, every engine is deterministic — no agents,
no LLM calls. Engines consume typed agent outputs and produce typed artifacts.
"""
